// TODO: we currently cannot fetch elements from thos TS files in node_modules
// import StraightEdge from "react-flow-renderer/dist/components/Edges/StraightEdge";

export const edgeTypes = {
    // default: StraightEdge,
    // step: SmoothStepEdge,
};
