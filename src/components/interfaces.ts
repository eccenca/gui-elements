export interface TestableComponent {
    /**
     * Can be set to use it as selector in test environments.
     * It is also possible to use `data-testid`.
     */
    "data-test-id"?: string;
    "data-testid"?: string;
}
